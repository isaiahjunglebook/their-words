# Taxonomy Changelog

Every tag created, merged, split, or retired — dated, with the reason.
The registry itself lives in `tags.md`.

---

## 2026-07-16 — Initial seed

Created the eight seed tags from the build spec: `the-lie`, `deferral`
(with tense note), `father`, `map-language-adopted`, `dream-outcome`,
`objection`, `testimonial-seed`, `founder-story-resonance`.
Reason: initial registry at build time.
